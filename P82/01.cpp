#include?<iostream>
using? namespace?std;

class?FunObj
{
public:
    //类重载了operator()调用运算符，第二个括号()表示参数
????void? operator()()
????{
????????cout?<<? "hello!"?<<?endl;
????}

?void? operator()(int n)
????{
????????cout?<<?"hello2!"?<<?endl;
????}
};

int?main()
{
????FunObj?fo;//类对象fo调用函数对象运算符
????fo();//类对象使用起来是一个函数，这个类对象就称之为函数对象
    fo(1);

    //构造一个匿名对象FunObj(),调用默认构造函数，带一个参数
    //然后类对象调用括号运算符，函数调用运算符
    FunObj()();
????return? 0;
}